name: untitled2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.8.0
  provider: ^6.1.2
  syncfusion_flutter_gauges: ^20.4.48
  firebase_database: ^10.0.0
  cupertino_icons: ^1.0.8
  connectivity_plus:
  json_annotation: ^4.9.0
  bloc:
  flutter_bloc: ^8.1.1
dev_dependencies:
  flutter_launcher_icons: "^0.9.2"
  build_runner: ^2.3.3
  json_serializable: ^6.6.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/IMG-20211017-WA0070 (1).png"

flutter:
  uses-material-design: true
  assets:
    - assets/
